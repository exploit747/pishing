SocialFish
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
python3 SocialFish.py root toor
lalu klik link di terminal

root and toor is account login
https://www.youtube.com/watch?v=LVlqZth1yPM
